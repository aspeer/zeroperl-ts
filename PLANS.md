# ZeroPerl TypeScript implementation plan

1. Consolidate the WebDyne bridge behaviour in this canonical repository.
2. Validate the bundled Perl 5.44.0 standard artifact and generated outputs.
3. Exercise the bridge with each supported runtime artifact from `zeroperl`.
4. Integrate the versioned artifacts with `wasm-WebDyne-PAGI` without changing
   the public package namespace in this milestone.

Publication and pushes require separate approval.
